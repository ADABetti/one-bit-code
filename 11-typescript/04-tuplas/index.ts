//As tuplas delimitam o tamanho do array:
// São utilizadas para valores previsíveis.
let crew: [string, string, string];

crew[0] = "Alice";
crew[1] = "Sofia";
crew[2] = "Letícia";

console.log(crew);

let point: [number, number];
point = [1, "teste"]; // não aceita valor diferente do que foi especificado.
//Porém, posso especificar diferentes tipos no array
